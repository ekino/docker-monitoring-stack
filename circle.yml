machine:
  services:
    - docker

dependencies:
  override:
    - ./helper.sh clear:all build:nocache run

test:
  pre:
    - sleep 30
    - docker logs influx
    - docker logs grafana
    - docker logs collectd
  override:
    - curl -sS localhost
